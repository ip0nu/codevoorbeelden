<!DOCTYPE html>
<html lang="en" class="trh">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Gatekeepr Login</title>

    <!-- Bootstrap -->
    <link href="//static.atpi.com/bootstrap/css/3.3.6/bootstrap.min.css" rel="stylesheet">
    <link href="//static.atpi.com/bootstrap/stylesheets/style_atpi_travelhub.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
    <script src="//static.atpi.com/bootstrap/js/jquery-1.9.1.min.js"></script>
    <script src="//static.atpi.com/bootstrap/js/jquery-ui.min.js"></script>
    <script src="//static.atpi.com/bootstrap/js/3.3.6/bootstrap.min.js"></script>
    <script src="//static.atpi.com/bootstrap/js/bootstrap-switch.min.js"></script>
</head>
<body class="trh">
<nav class="headerbar">
    <div class="col-sm-6 brand"><img src="//static.atpi.com/bootstrap/img/logo_gatekeepr.png" width="162" height="55" border="0" /></div>
</nav>
<div class="col-sm-12 no-pad-left no-pad-right">
    <div id="custom-bootstrap-topmenu" class="navbar"></div>
</div>

<div class="container login">
    <div class="col-md-4 col-md-offset-4">
        <div class="loginWrapper">
            <h2 class="titleHeader icon-user margin-bottom-20">Login</h2>

            <div>
                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    {{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |
                    <a href="{{ path('fos_user_security_logout') }}">
                        {{ 'layout.logout'|trans({}, 'FOSUserBundle') }}
                    </a>
                {% endif %}
            </div>

            {% if app.request.hasPreviousSession %}
                {% for type, messages in app.session.flashbag.all() %}
                    {% for message in messages %}
                        <div class="flash-{{ type }}">
                            {{ message }}
                        </div>
                    {% endfor %}
                {% endfor %}
            {% endif %}

            <div>
                {% block fos_user_content %}
                {% endblock fos_user_content %}
            </div>

        </div>
    </div>
</div>
<script src="//static.atpi.com/bootstrap/js/3.3.6/bootstrap.min.js"></script>
</body>
</html>